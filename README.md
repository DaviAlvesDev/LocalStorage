# LocalStorage
Este projeto simula uma lista de pedidos (Tapas Menu) onde os dados são persistidos utilizando a API de LocalStorage. O desafio foca na manipulação de arrays de objetos e na técnica de Event Delegation para lidar com elementos criados dinamicamente no DOM.
